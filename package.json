{
  "name": "jpg-2000-parse",
  "version": "1.0.0",
  "main": "api/convertAndSaveImage.js",
  "dependencies": {
    "express": "^4.17.1",
    "body-parser": "^1.19.0"
  },
  "scripts": {
    "postinstall": "curl -L https://github.com/uclouvain/openjpeg/releases/download/${OPENJPEG_VERSION}/openjpeg-${OPENJPEG_VERSION}-linux-x86_64.tar.gz | tar xz -C . --strip-components=1"
  }
}
